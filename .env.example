# ── GST Portal Credentials ──────────────────────────
GST_USERNAME=your_gstin_username
GST_PASSWORD=your_portal_password

# ── Profile folder ───────────────────────────────────
# Chrome profile stored here — persists device recognition across runs
# After first login (OTP once), subsequent logins need CAPTCHA only
PROFILE_DIR=./profile

# ── Phase 2 (not needed yet) ─────────────────────────
# CAPTCHA_PROVIDER=2captcha
# CAPTCHA_API_KEY=your_key_here
